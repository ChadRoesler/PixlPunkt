<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="PixlPunkt.UI.Controls.CollapsibleSplitter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ic="using:FluentIcons.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">
        <!-- Main splitter area with grip -->
        <Rectangle Fill="{ThemeResource LayerFillColorDefaultBrush}"
                   HorizontalAlignment="Stretch"
                   VerticalAlignment="Stretch"/>
        
        <!-- Collapse/Expand Button - positioned at center, NO grip indicator when expanded -->
        <Border x:Name="CollapseButton"
                Width="12"
                Height="48"
                Padding="0"
                Margin="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="{ThemeResource ControlFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="4"
                ToolTipService.ToolTip="Collapse/Expand Panel">
            <ic:FluentIcon x:Name="CollapseIcon" 
                           Icon="ChevronLeft" 
                           FontSize="10"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </Border>
    </Grid>
</UserControl>

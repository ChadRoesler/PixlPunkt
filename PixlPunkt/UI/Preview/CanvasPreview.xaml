<UserControl
    x:Class="PixlPunkt.UI.Preview.CanvasPreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:skia="using:SkiaSharp.Views.Windows"
    xmlns:ic="using:FluentIcons.WinUI">

    <Grid x:Name="Root" RowSpacing="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                <Button x:Name="ZoomInButton" Padding="3">
                    <ic:FluentIcon Icon="ZoomIn" FontSize="16"/>
                </Button>
                <Button x:Name="ZoomOutButton" Padding="3">
                    <ic:FluentIcon Icon="ZoomOut" FontSize="16"/>
                </Button>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="FitButton" Padding="3">
                    <ic:FluentIcon Icon="ZoomFit" FontSize="16"/>
                </Button>
            </StackPanel>
        </Grid>


        <!-- Preview area -->
        <Border Grid.Row="1"
                x:Name="LayerBox"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8"
                Margin="-3,0,-3,-3">
            <!-- Container for canvas element - created in code-behind (SKCanvasElement on Uno, SKXamlCanvas on WinAppSdk) -->
            <Grid x:Name="PreviewCanvasContainer"
                  PointerPressed="PreviewCanvas_PointerPressed"
                  PointerMoved="PreviewCanvas_PointerMoved"
                  PointerReleased="PreviewCanvas_PointerReleased"
                  PointerExited="PreviewCanvas_PointerExited" />
        </Border>
    </Grid>
</UserControl>
